# Docker Environment Configuration for IntelliSSH
# Copy this file to .env and configure the values for your deployment

# Required: Security Keys
JWT_SECRET=your_jwt_secret_change_in_production_minimum_32_characters
ENCRYPTION_KEY=your_32_byte_hex_encryption_key_change_in_production

# OIDC/SSO Configuration (Optional - for single sign-on)
# Uncomment and configure if you want to use OIDC authentication

# OIDC_ISSUER=https://your-oidc-provider.com
# OIDC_CLIENT_ID=your_client_id
# OIDC_CLIENT_SECRET=your_client_secret
# OIDC_ADMIN_GROUP_SCOPE=admin_group_name

# Registration Control
# Set to true if you want to disable local user registration (recommended with OIDC)
DISABLED_SIGNUP=false

# LLM Helper Configuration (Optional)
# Configure if you want AI assistance features

LLM_PROVIDER=openai
# OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Alternative: Ollama (local LLM)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Example OIDC Configurations:

# Google OIDC:
# OIDC_ISSUER=https://accounts.google.com
# OIDC_CLIENT_ID=your-google-client-id.googleusercontent.com
# OIDC_CLIENT_SECRET=your-google-client-secret

# Azure AD / Entra ID:
# OIDC_ISSUER=https://login.microsoftonline.com/your-tenant-id/v2.0
# OIDC_CLIENT_ID=your-azure-client-id
# OIDC_CLIENT_SECRET=your-azure-client-secret
# OIDC_ADMIN_GROUP_SCOPE=admin-group-object-id

# Keycloak:
# OIDC_ISSUER=https://your-keycloak.com/auth/realms/your-realm
# OIDC_CLIENT_ID=intellissh-client
# OIDC_CLIENT_SECRET=your-keycloak-client-secret
# OIDC_ADMIN_GROUP_SCOPE=/admin